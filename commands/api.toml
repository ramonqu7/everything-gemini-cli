description = "Design or review API endpoints"
prompt = """
Help me design/review the API for:

{{args}}

Consider: REST conventions, request/response schemas, error handling, pagination, authentication, rate limiting, and versioning.
"""
